{
    # Constants shared across every module. Not options: these are never
    # overridden per-host, they exist so identifiers aren't retyped everywhere.
    _module.args.user = {
        primary = "caligula";
        media = "media";
    };
}
