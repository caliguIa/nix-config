{
  config,
  pkgs,
  lib,
  ...
}:

{
  # Program configurations specific to NixOS
  # Add any NixOS-specific home-manager program configurations here
  # For example:
  # alacritty = { } // import ../shared/packages/alacritty.nix { inherit pkgs; };
}